{
  "name": "retro-api",
  "version": "1.0.0",
  "description": "Retrospective Management REST API — local JSON storage",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/**/*.test.js",
    "verify": "npm test",
    "seed:demo": "node scripts/seed-demo-retros.js",
    "retro:status": "node scripts/list-retro-state.js",
    "export:reminder": "node scripts/export-reminder-snapshot.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
